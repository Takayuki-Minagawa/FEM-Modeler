# Thermal visualization regression fixture

Generated on 2026-09-13 by the digest-pinned DOLFINx runtime in `solver-tests/` using the one-direction heat-conduction benchmark with volumetric generation. The full numerical checks are recorded in `docs/solver-validation.json`.

The source project and result package preserve identical provenance and explicit mesh IDs. They are intentionally small enough for the default browser tests. Regenerate both files together with `solver-tests/run.py` when changing the input contract; do not edit a stored fingerprint to force a match.
